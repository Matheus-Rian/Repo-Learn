const operations = {
  sum: (x, y) => x + y,
  sub: (x, y) => x - y,
  div: (x, y) => x / y,
  mult: (x, y) => x * y,
}

console.log(operations.sum(2, 2));
console.log(operations.sub(2, 2));
console.log(operations.div(2, 2));
console.log(operations.mult(2, 2));
